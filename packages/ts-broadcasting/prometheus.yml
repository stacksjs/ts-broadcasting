# Prometheus configuration for ts-broadcasting
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'broadcasting-server'
    static_configs:
      - targets: ['broadcasting:6001']
    metrics_path: '/metrics'
    scrape_interval: 10s
